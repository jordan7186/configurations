# ~/.zprofile (Linux)
# Sourced for login shells. Keep to PATH/env only.

# User-level binaries: starship, zoxide, fzf, herdr, eza, bat
export PATH="$HOME/.local/bin:$HOME/.local/share/fzf/bin:$PATH"
